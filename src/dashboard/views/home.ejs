<!DOCTYPE html>
<html>
<head>
	<title>1A Administration Dashboard</title>
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
	<link rel="icon" href="/favicon.ico">
	<link rel="stylesheet" href="/custom.css">
	<style>
		body { background: #181824; color: #fff; min-height: 100vh; }
		.hero {
			padding: 4rem 0 2rem 0;
			text-align: center;
		}
		.hero-title {
			font-size: 2.5rem;
			font-weight: 800;
			color: #fff;
			letter-spacing: 0.02em;
		}
		.hero-desc {
			font-size: 1.2rem;
			color: #bdbdbd;
			margin-bottom: 2rem;
		}
		.quick-links {
			margin-top: 2rem;
			display: flex;
			justify-content: center;
			gap: 1.5rem;
			flex-wrap: wrap;
		}
		.quick-link {
			background: #232336;
			color: #fff;
			border-radius: 1rem;
			padding: 1.2rem 2.2rem;
			font-size: 1.1rem;
			font-weight: 600;
			text-decoration: none;
			box-shadow: 0 2px 12px #0003;
			transition: background 0.15s, color 0.15s, box-shadow 0.15s;
		}
		.quick-link:hover {
			background: #3c00a3;
			color: #fff;
			box-shadow: 0 4px 24px #0005;
			text-decoration: none;
		}
	</style>
</head>
<body>
	<%- include("partials/navbar", { user, active }) %>
	<div id="main-content">
		<div class="container hero">
			<h1 class="hero-title mb-3">Welcome to the 1A Administration Dashboard</h1>
			<p class="hero-desc">
				Manage events, certifications, and more for your Discord community.<br>
				Use the quick links below to get started.
			</p>
			<div class="quick-links">
				<a href="/events" class="quick-link">View Events</a>
				<a href="/certs" class="quick-link">View Certifications</a>
				<% if (user) { %>
					<a href="/profile" class="quick-link">Your Profile</a>
				<% } else { %>
					<a href="/login" class="quick-link">Login with Discord</a>
				<% } %>
			</div>
		</div>
	</div>
	<%- include("partials/footer") %>
</body>
</html>