<!DOCTYPE html>
<html>
<head>
	<title>Profile</title>
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
	<link rel="icon" href="/favicon.ico">
	<link rel="stylesheet" href="/custom.css">
	<style>
		:root { --bs-primary: #3c00a3; }
		body { background: #181824; color: #fff; }
		.card, .modal-content { background: #232336; color: #fff; }
	</style>
</head>
<body>
	<%- include("partials/navbar", { user }) %>
	<div class="container mt-5">
		<div class="card shadow-sm mx-auto" style="max-width: 400px;">
			<div class="card-body text-center">
				<img src="<%= user.avatar ? `https://cdn.discordapp.com/avatars/${user.id}/${user.avatar}.png` : '' %>" width="96" class="rounded-circle mb-3">
				<h3 class="card-title"><%= user.username %></h3>
				<p class="card-text text-muted">User ID: <%= user.id %></p>
				<a href="/" class="btn btn-primary">Back to home</a>
			</div>
		</div>
	</div>
	<%- include("partials/footer") %>
</body>
</html>